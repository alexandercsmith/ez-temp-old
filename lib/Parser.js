const Schema = require('./Schema');
const Errors = require('./Errors');

/**
 * @func        Parser.table
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.table = async (data, options={}) => {
  // await
};

/**
 * @func        Parser.global
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.global = async (data, options={}) => {
  // await
};

/**
 * @func        Parser.list
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.list = async (data, options={}) => {
  // await
};

/**
 * @func        Parser.item
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.item = async (data, options={}) => {
  // await
};

/**
 * @func        Parser.batch
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.batch = async (data, options={}) => {
  // await
};

/**
 * @func        Parser.transact
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.transact = async (data, options={}) => {
  // await
};

/**
 * @func        Parser.backup
 * @constructor
 * @requires    {Schema}
 * @param       {object} data
 * @param       {object} options
 * @prop        {string} options.table
 * @throws      {error}
 */
exports.backup = async (data, options={}) => {
  // await
};
